# collection META

- Sub-components of the generic admin CollectionView.
- Card/table renderers + toolbar + pagination + select bulk actions.
- Reusable across every declarative content type's collection surface.
- NOT a place for extension-specific list layouts — those belong in `extensions/<id>/...`.
