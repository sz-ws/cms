# repo META

- `src/`: core runtime + Next.js app
- `extensions/<id>/`: compile-time code extensions (own admin/public/api/kit)
- `registry/extensions/<id>/`: declarative manifests + admin/sql sources
- `cms/`: mirrored dev seed copies (kept under sync with `registry/`)

Read `docs/core-v2-architecture.md` for the architecture source of truth.
